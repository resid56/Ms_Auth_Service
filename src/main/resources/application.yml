server:
  port: 9010
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/ms_auth
    username: root
    password: 000111rrr
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  mvc:
   path match:
    matching-strategy: ant_path_matcher
Big3:
 app:
  secret-key: bezCoderSecretKey
  expiration-ms: 86400000
